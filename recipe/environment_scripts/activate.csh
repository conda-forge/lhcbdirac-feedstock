#!/usr/bin/env csh

setenv X509_VOMS_DIR "${CONDA_PREFIX}/etc/grid-security/vomsdir"
setenv X509_CERT_DIR "${CONDA_PREFIX}/etc/grid-security/certificates"
setenv X509_VOMSES "${CONDA_PREFIX}/etc/grid-security/vomses"

